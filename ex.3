// je crée une variable modifiable nommée "starter" à laquelle j'attribue pour l'exemple la valeur de 68.

let starter = 68;

// Pour une variable modifiable nommée "Calculator", j'attribue comme valeur, la variable "starter" à laquelle je rajoute +1.
// Tant que la variable "calculator" n'a pas atteint pas le nombre "starter +11", alors la variable "calculator" doit continuer d'augmenter.

for (let calculator = starter +1; 
			calculator < starter +11; 
      calculator++) 
      
// J'affiche le résultat.
      
  console.log(calculator)
