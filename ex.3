/* Exercice 3
Ecrire un algorithme qui demade un nombre de départ,
et qui ensuite affiche les dix nombres suivants.
Par exemple, si l'utilisateur entre le nombre 17,
 le programme affichera les nombres de 18 à 27.*/

 function augment ()
 {
    let nombre = 5;
    arret = nombre + 11;

    while (nombre < arret){
        console.log(nombre);
        nombre++;
    }
 }

augment();
